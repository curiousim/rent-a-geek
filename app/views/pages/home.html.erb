<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>
         <%= form_tag(geeks_path, { method: :get, :class => 'form-inline '}) do %>
          <%= text_field_tag(:search, params[:search]) %>
          <%= submit_tag ("search"), class: 'btn btn-sm btn-light' %>
          <% end %>